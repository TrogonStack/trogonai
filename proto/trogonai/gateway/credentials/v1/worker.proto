edition = "2024";

package trogonai.gateway.credentials.v1;

message CredentialLifecycleRecoveryWorkerCheckpoint {
  uint64 last_scanned_sequence = 1;
  uint32 consecutive_failure_count = 2;
  uint64 first_failure_unix_seconds = 3;
  uint64 retry_after_unix_seconds = 4;
}
