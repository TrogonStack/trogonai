edition = "2024";

package trogonai.scheduler.schedules.v1;

// SchedulePaused records the desired lifecycle state as paused.
message SchedulePaused {
  // Stable NATS token for the schedule stream being paused.
  string schedule_id = 1 [features.field_presence = LEGACY_REQUIRED];
}
