---
term: "WASM"
section: "WebAssembly execution"
order: 0
---

# WASM

WebAssembly. The decider logic can be compiled to a WASM component and executed
by a host that shares the exact same `decide` / `evolve` semantics as the native
path. See [Decider Platform](../architecture/decider.md#the-wasm-path).
