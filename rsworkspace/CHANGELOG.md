# Changelog

All notable changes to trogon are documented in this file.

## [0.1.0] - 2026-06-15

### Added

- REPL slash commands: `/release-notes`, `/bug`, `/tasks`, `/agents`, `/checkpoint`, `/rewind`
- Cross-runner model switching with session export/import via `CrossRunnerSwitcher`
- Permission modes and tool approval gates for safer agent execution

### Changed

- Phase 3 CLI parity work toward Claude Code substitution (REPL commands, config, status)
