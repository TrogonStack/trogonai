[package]
name = "trogon-cron-jobs-proto"
version = "0.1.0"
edition = "2024"

[lints.rust]
warnings = "deny"
unexpected_cfgs = { level = "deny", check-cfg = ['cfg(coverage)'] }

[lints.clippy]
all = "deny"

[dependencies]
protobuf = { workspace = true }
serde = { workspace = true }
trogon-eventsourcing = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
