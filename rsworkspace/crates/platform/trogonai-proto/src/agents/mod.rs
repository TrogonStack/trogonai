// The product area (`agents`) and the aggregate (`agents`) share a name here
// because the brief mandates `proto/trogonai/agents/agents/v1/` verbatim
// (the product area is the aggregate for this stream).
#[allow(clippy::module_inception)]
pub mod agents;
