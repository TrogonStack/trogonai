// @generated by buffa-codegen. DO NOT EDIT.
// source: trogonai/session/sessions/v1alpha1/resource_access.proto

/// ResourceAccessRecord records what a tool call did to a namespace of
/// resources, as distinct from what content it read.
///
/// It exists because ResourceObservation deliberately answers only one question.
/// An observation means content entered the model's context, and its digest is
/// what makes a later write checkable. A search that walks a directory of
/// sensitive filenames and returns none of their contents produces no
/// observation, correctly, and yet something happened that a compliance reviewer
/// has to be able to see: the agent learned that those files exist and what they
/// are called.
///
/// The two must stay separate rather than merging into one weakened observation.
/// "Did the agent read this file" and "did the agent see this file's name" are
/// different questions with different answers and different consequences, and a
/// single record that means either makes both unanswerable.
///
/// Records are per-scope, never per-path. That is the same volume argument that
/// keeps reads off their own event: a search can walk forty thousand paths, and
/// forty thousand facts appended to a log that is never truncated
/// (ADR#0035 facet 7) would make the dominant content of a session stream the
/// output of one grep. The scope plus the counts is what a reviewer needs to know
/// what was exposed; the path list itself, when it is worth keeping, goes out of
/// line as an artifact and is erasable like any other artifact while the counts
/// survive it.
#[derive(Clone, Debug, Default)]
pub struct ResourceAccessRecordView<'a> {
    /// Field 1: `action`
    pub action: ::buffa::EnumValue<super::super::ResourceAction>,
    /// The namespace this access covered, as a URI prefix or a glob in the same URI
    /// form as WorkspaceRef.uri. This is the extent of exposure, not a single
    /// resource: a directory listing's scope is the directory, and a recursive
    /// search's scope is its root.
    ///
    /// Field 2: `scope`
    pub scope: &'a str,
    /// Resources within scope the tool actually enumerated back to the caller. For
    /// a search this is the hit count, which is the number that matters, since
    /// knowing that four files in a secrets directory matched "password" is a
    /// different disclosure from knowing the directory has four hundred files.
    ///
    /// Field 3: `matched`
    pub matched: u64,
    /// Resources within scope the tool traversed to produce that answer. Recorded
    /// apart from `matched` because traversal is itself access to a namespace: a
    /// filter that rejected a path still required reading the path's name.
    ///
    /// Field 4: `traversed`
    pub traversed: u64,
    /// True when the tool covered its scope entirely. False when it stopped at a
    /// result limit, a depth limit, or a timeout, so a reviewer never reads a
    /// truncated enumeration as a complete inventory of what was exposed.
    ///
    /// Field 5: `complete`
    pub complete: bool,
    /// Claim-check to the enumerated resource identifiers, when they were kept.
    ///
    /// Unset is ordinary and does not weaken the record: the counts and the scope
    /// stand on their own. It is a claim-check rather than a repeated string for
    /// the volume reason above, and being an artifact is also what makes the list
    /// erasable under a deletion request while leaving the audit fact that an
    /// enumeration happened permanently intact.
    ///
    /// Field 6: `enumerated`
    pub enumerated: ::buffa::MessageFieldView<
        super::super::__buffa::view::ArtifactRefView<'a>,
    >,
    #[doc(hidden)]
    pub __buffa_required_seen_0: u64,
}
impl<'a> ResourceAccessRecordView<'a> {
    /**Whether required field `action` was present on the wire.

Distinguishes a field that was absent from one explicitly encoded with its default value (required scalar fields are stored as bare, non-`Option` types, so the value alone cannot tell the two apart). Presence is recorded only by the wire decoder: a default or hand-built view reports `false`. Encoding is unaffected — required fields are always written.*/
    #[must_use]
    #[inline]
    pub const fn has_action(&self) -> bool {
        self.__buffa_required_seen_0 & 1u64 != 0
    }
    /**Whether required field `scope` was present on the wire.

Distinguishes a field that was absent from one explicitly encoded with its default value (required scalar fields are stored as bare, non-`Option` types, so the value alone cannot tell the two apart). Presence is recorded only by the wire decoder: a default or hand-built view reports `false`. Encoding is unaffected — required fields are always written.*/
    #[must_use]
    #[inline]
    pub const fn has_scope(&self) -> bool {
        self.__buffa_required_seen_0 & 2u64 != 0
    }
    /**Whether required field `matched` was present on the wire.

Distinguishes a field that was absent from one explicitly encoded with its default value (required scalar fields are stored as bare, non-`Option` types, so the value alone cannot tell the two apart). Presence is recorded only by the wire decoder: a default or hand-built view reports `false`. Encoding is unaffected — required fields are always written.*/
    #[must_use]
    #[inline]
    pub const fn has_matched(&self) -> bool {
        self.__buffa_required_seen_0 & 4u64 != 0
    }
    /**Whether required field `traversed` was present on the wire.

Distinguishes a field that was absent from one explicitly encoded with its default value (required scalar fields are stored as bare, non-`Option` types, so the value alone cannot tell the two apart). Presence is recorded only by the wire decoder: a default or hand-built view reports `false`. Encoding is unaffected — required fields are always written.*/
    #[must_use]
    #[inline]
    pub const fn has_traversed(&self) -> bool {
        self.__buffa_required_seen_0 & 8u64 != 0
    }
    /**Whether required field `complete` was present on the wire.

Distinguishes a field that was absent from one explicitly encoded with its default value (required scalar fields are stored as bare, non-`Option` types, so the value alone cannot tell the two apart). Presence is recorded only by the wire decoder: a default or hand-built view reports `false`. Encoding is unaffected — required fields are always written.*/
    #[must_use]
    #[inline]
    pub const fn has_complete(&self) -> bool {
        self.__buffa_required_seen_0 & 16u64 != 0
    }
}
impl<'a> ::buffa::MessageView<'a> for ResourceAccessRecordView<'a> {
    type Owned = super::super::ResourceAccessRecord;
    fn decode_view(buf: &'a [u8]) -> ::core::result::Result<Self, ::buffa::DecodeError> {
        let __limit = ::core::cell::Cell::new(::buffa::DEFAULT_UNKNOWN_FIELD_LIMIT);
        <Self as ::buffa::MessageView>::decode_view_ctx(
            buf,
            ::buffa::DecodeContext::new(::buffa::RECURSION_LIMIT, &__limit),
        )
    }
    fn decode_view_with_ctx(
        buf: &'a [u8],
        ctx: ::buffa::DecodeContext<'_>,
    ) -> ::core::result::Result<Self, ::buffa::DecodeError> {
        <Self as ::buffa::MessageView>::decode_view_ctx(buf, ctx)
    }
    #[inline]
    fn merge_view_field(
        &mut self,
        tag: ::buffa::encoding::Tag,
        cur: &'a [u8],
        _before_tag: &'a [u8],
        ctx: ::buffa::DecodeContext<'_>,
    ) -> ::core::result::Result<&'a [u8], ::buffa::DecodeError> {
        let _ = ctx;
        #[allow(unused_variables)]
        let view = self;
        let mut cur = cur;
        match tag.field_number() {
            1u32 => {
                ::buffa::encoding::check_wire_type(
                    tag,
                    ::buffa::encoding::WireType::Varint,
                )?;
                view.action = ::buffa::EnumValue::from(
                    ::buffa::types::decode_int32(&mut cur)?,
                );
                view.__buffa_required_seen_0 |= 1u64;
            }
            2u32 => {
                ::buffa::encoding::check_wire_type(
                    tag,
                    ::buffa::encoding::WireType::LengthDelimited,
                )?;
                view.scope = ::buffa::types::borrow_str(&mut cur)?;
                view.__buffa_required_seen_0 |= 2u64;
            }
            3u32 => {
                ::buffa::encoding::check_wire_type(
                    tag,
                    ::buffa::encoding::WireType::Varint,
                )?;
                view.matched = ::buffa::types::decode_uint64(&mut cur)?;
                view.__buffa_required_seen_0 |= 4u64;
            }
            4u32 => {
                ::buffa::encoding::check_wire_type(
                    tag,
                    ::buffa::encoding::WireType::Varint,
                )?;
                view.traversed = ::buffa::types::decode_uint64(&mut cur)?;
                view.__buffa_required_seen_0 |= 8u64;
            }
            5u32 => {
                ::buffa::encoding::check_wire_type(
                    tag,
                    ::buffa::encoding::WireType::Varint,
                )?;
                view.complete = ::buffa::types::decode_bool(&mut cur)?;
                view.__buffa_required_seen_0 |= 16u64;
            }
            6u32 => {
                ::buffa::encoding::check_wire_type(
                    tag,
                    ::buffa::encoding::WireType::LengthDelimited,
                )?;
                let __sub_ctx = ctx.descend()?;
                let sub = ::buffa::types::borrow_bytes(&mut cur)?;
                match view.enumerated.as_mut() {
                    Some(existing) => {
                        ::buffa::MessageView::merge_into_view(existing, sub, __sub_ctx)?
                    }
                    None => {
                        view.enumerated = ::buffa::MessageFieldView::set(
                            <super::super::__buffa::view::ArtifactRefView as ::buffa::MessageView>::decode_view_ctx(
                                sub,
                                __sub_ctx,
                            )?,
                        );
                    }
                }
            }
            _ => {
                ::buffa::encoding::skip_field_depth(tag, &mut cur, ctx.depth())?;
            }
        }
        ::core::result::Result::Ok(cur)
    }
    fn to_owned_message(
        &self,
    ) -> ::core::result::Result<
        super::super::ResourceAccessRecord,
        ::buffa::DecodeError,
    > {
        self.to_owned_from_source(None)
    }
    #[allow(clippy::useless_conversion, clippy::needless_update)]
    fn to_owned_from_source(
        &self,
        __buffa_src: ::core::option::Option<&::buffa::bytes::Bytes>,
    ) -> ::core::result::Result<
        super::super::ResourceAccessRecord,
        ::buffa::DecodeError,
    > {
        #[allow(unused_imports)]
        use ::buffa::alloc::string::ToString as _;
        let _ = __buffa_src;
        ::core::result::Result::Ok(super::super::ResourceAccessRecord {
            action: self.action,
            scope: self.scope.to_string(),
            matched: self.matched,
            traversed: self.traversed,
            complete: self.complete,
            enumerated: match self.enumerated.as_option() {
                Some(v) => {
                    ::buffa::MessageField::<
                        super::super::ArtifactRef,
                        ::buffa::Inline<super::super::ArtifactRef>,
                    >::some(v.to_owned_from_source(__buffa_src)?)
                }
                None => ::buffa::MessageField::none(),
            },
            ..::core::default::Default::default()
        })
    }
}
impl<'a> ::buffa::ViewEncode<'a> for ResourceAccessRecordView<'a> {
    #[allow(clippy::needless_borrow, clippy::let_and_return)]
    fn compute_size(&self, __cache: &mut ::buffa::SizeCache) -> u32 {
        #[allow(unused_imports)]
        use ::buffa::Enumeration as _;
        let mut size = 0u64;
        {
            let val = self.action.to_i32();
            size += 1u64 + ::buffa::types::int32_encoded_len(val) as u64;
        }
        size += 1u64 + ::buffa::types::string_encoded_len(&self.scope) as u64;
        size += 1u64 + ::buffa::types::uint64_encoded_len(self.matched) as u64;
        size += 1u64 + ::buffa::types::uint64_encoded_len(self.traversed) as u64;
        size += 1u64 + ::buffa::types::BOOL_ENCODED_LEN as u64;
        if self.enumerated.is_set() {
            let __slot = __cache.reserve();
            let inner_size = self.enumerated.compute_size(__cache);
            __cache.set(__slot, inner_size);
            size
                += 1u64 + ::buffa::encoding::varint_len(inner_size as u64) as u64
                    + inner_size as u64;
        }
        ::buffa::saturate_size(size)
    }
    #[allow(clippy::needless_borrow)]
    fn write_to(
        &self,
        __cache: &mut ::buffa::SizeCache,
        buf: &mut impl ::buffa::EncodeSink,
    ) {
        #[allow(unused_imports)]
        use ::buffa::Enumeration as _;
        ::buffa::types::put_int32_field(1u32, self.action.to_i32(), buf);
        ::buffa::types::put_string_field(2u32, &self.scope, buf);
        ::buffa::types::put_uint64_field(3u32, self.matched, buf);
        ::buffa::types::put_uint64_field(4u32, self.traversed, buf);
        ::buffa::types::put_bool_field(5u32, self.complete, buf);
        if self.enumerated.is_set() {
            ::buffa::types::put_len_delimited_header(
                6u32,
                u64::from(__cache.consume_next()),
                buf,
            );
            self.enumerated.write_to(__cache, buf);
        }
    }
}
/// Serializes this view as protobuf JSON.
///
/// Implicit-presence fields with default values are omitted, `required`
/// fields are always emitted, explicit-presence (`optional`) fields are
/// emitted only when set, bytes fields are base64-encoded, and enum
/// values are their proto name strings.
///
/// This impl uses `serialize_map(None)` because the number of emitted
/// fields depends on default-omission rules; serializers that require
/// known map lengths (e.g. `bincode`) will return a runtime error.
/// Use the owned message type for those formats.
impl<'__a> ::serde::Serialize for ResourceAccessRecordView<'__a> {
    fn serialize<__S: ::serde::Serializer>(
        &self,
        __s: __S,
    ) -> ::core::result::Result<__S::Ok, __S::Error> {
        use ::serde::ser::SerializeMap as _;
        let mut __map = __s.serialize_map(::core::option::Option::None)?;
        {
            __map.serialize_entry("action", &self.action)?;
        }
        {
            __map.serialize_entry("scope", self.scope)?;
        }
        {
            __map
                .serialize_entry(
                    "matched",
                    &::buffa::json_helpers::ProtoJson(&self.matched),
                )?;
        }
        {
            __map
                .serialize_entry(
                    "traversed",
                    &::buffa::json_helpers::ProtoJson(&self.traversed),
                )?;
        }
        {
            __map.serialize_entry("complete", &self.complete)?;
        }
        {
            if let ::core::option::Option::Some(__v) = self.enumerated.as_option() {
                __map.serialize_entry("enumerated", __v)?;
            }
        }
        __map.end()
    }
}
impl<'a> ::buffa::MessageName for ResourceAccessRecordView<'a> {
    const PACKAGE: &'static str = "trogonai.session.sessions.v1alpha1";
    const NAME: &'static str = "ResourceAccessRecord";
    const FULL_NAME: &'static str = "trogonai.session.sessions.v1alpha1.ResourceAccessRecord";
    const TYPE_URL: &'static str = "type.googleapis.com/trogonai.session.sessions.v1alpha1.ResourceAccessRecord";
}
::buffa::impl_default_view_instance!(ResourceAccessRecordView);
::buffa::impl_view_reborrow!(ResourceAccessRecordView);
/** Self-contained, `'static` owned view of a `ResourceAccessRecord` message.

 Wraps [`::buffa::OwnedView`]`<`[`ResourceAccessRecordView`]`<'static>>`: the decoded view and the [`::buffa::bytes::Bytes`] buffer it borrows from travel together, so the handle is `'static` and `Send + Sync` — suitable for async handlers, spawned tasks, and anywhere a `'static` bound is required.

 Field accessors return borrows tied to `&self`. Use [`Self::view`] to get the full [`ResourceAccessRecordView`] when you need struct patterns, iteration helpers, or to pass the view to lifetime-parameterised code.*/
#[derive(Clone, Debug)]
pub struct ResourceAccessRecordOwnedView(
    ::buffa::OwnedView<ResourceAccessRecordView<'static>>,
);
impl ResourceAccessRecordOwnedView {
    /// Decode an owned view from a [`::buffa::bytes::Bytes`] buffer.
    ///
    /// The view borrows directly from the buffer's data; the buffer is
    /// retained inside the returned handle.
    ///
    /// # Errors
    ///
    /// Returns [`::buffa::DecodeError`] if the buffer contains invalid
    /// protobuf data.
    pub fn decode(
        bytes: ::buffa::bytes::Bytes,
    ) -> ::core::result::Result<Self, ::buffa::DecodeError> {
        ::core::result::Result::Ok(
            ResourceAccessRecordOwnedView(::buffa::OwnedView::decode(bytes)?),
        )
    }
    /// Decode with custom [`::buffa::DecodeOptions`] (recursion limit,
    /// max message size).
    ///
    /// # Errors
    ///
    /// Returns [`::buffa::DecodeError`] if the buffer is invalid or
    /// exceeds the configured limits.
    pub fn decode_with_options(
        bytes: ::buffa::bytes::Bytes,
        opts: &::buffa::DecodeOptions,
    ) -> ::core::result::Result<Self, ::buffa::DecodeError> {
        ::core::result::Result::Ok(
            ResourceAccessRecordOwnedView(
                ::buffa::OwnedView::decode_with_options(bytes, opts)?,
            ),
        )
    }
    /// Build from an owned message via an encode → decode round-trip.
    ///
    /// # Errors
    ///
    /// Returns [`::buffa::DecodeError::MessageTooLarge`] if the
    /// message's encoded size exceeds the 2 GiB protobuf limit, or
    /// another [`::buffa::DecodeError`] if the re-encoded bytes are
    /// somehow invalid (should not happen for well-formed messages).
    pub fn from_owned(
        msg: &super::super::ResourceAccessRecord,
    ) -> ::core::result::Result<Self, ::buffa::DecodeError> {
        ::core::result::Result::Ok(
            ResourceAccessRecordOwnedView(::buffa::OwnedView::from_owned(msg)?),
        )
    }
    /// Borrow the full [`ResourceAccessRecordView`] with its lifetime tied to `&self`.
    #[must_use]
    pub fn view(&self) -> &ResourceAccessRecordView<'_> {
        self.0.reborrow()
    }
    /// Convert to the owned message type.
    ///
    /// Infallible: this type's constructors wire-decode their
    /// buffer, and a view produced by wire decoding always
    /// converts. Delegates to [`::buffa::OwnedView::to_owned_message`],
    /// whose contract also governs handles converted from a raw
    /// [`::buffa::OwnedView`].
    #[must_use]
    pub fn to_owned_message(&self) -> super::super::ResourceAccessRecord {
        self.0.to_owned_message()
    }
    /// The underlying bytes buffer.
    #[must_use]
    pub fn bytes(&self) -> &::buffa::bytes::Bytes {
        self.0.bytes()
    }
    /// Consume the handle, returning the underlying bytes buffer.
    #[must_use]
    pub fn into_bytes(self) -> ::buffa::bytes::Bytes {
        self.0.into_bytes()
    }
    /// Field 1: `action`
    #[must_use]
    pub fn action(&self) -> ::buffa::EnumValue<super::super::ResourceAction> {
        self.0.reborrow().action
    }
    /// The namespace this access covered, as a URI prefix or a glob in the same URI
    /// form as WorkspaceRef.uri. This is the extent of exposure, not a single
    /// resource: a directory listing's scope is the directory, and a recursive
    /// search's scope is its root.
    ///
    /// Field 2: `scope`
    #[must_use]
    pub fn scope(&self) -> &'_ str {
        self.0.reborrow().scope
    }
    /// Resources within scope the tool actually enumerated back to the caller. For
    /// a search this is the hit count, which is the number that matters, since
    /// knowing that four files in a secrets directory matched "password" is a
    /// different disclosure from knowing the directory has four hundred files.
    ///
    /// Field 3: `matched`
    #[must_use]
    pub fn matched(&self) -> u64 {
        self.0.reborrow().matched
    }
    /// Resources within scope the tool traversed to produce that answer. Recorded
    /// apart from `matched` because traversal is itself access to a namespace: a
    /// filter that rejected a path still required reading the path's name.
    ///
    /// Field 4: `traversed`
    #[must_use]
    pub fn traversed(&self) -> u64 {
        self.0.reborrow().traversed
    }
    /// True when the tool covered its scope entirely. False when it stopped at a
    /// result limit, a depth limit, or a timeout, so a reviewer never reads a
    /// truncated enumeration as a complete inventory of what was exposed.
    ///
    /// Field 5: `complete`
    #[must_use]
    pub fn complete(&self) -> bool {
        self.0.reborrow().complete
    }
    /// Claim-check to the enumerated resource identifiers, when they were kept.
    ///
    /// Unset is ordinary and does not weaken the record: the counts and the scope
    /// stand on their own. It is a claim-check rather than a repeated string for
    /// the volume reason above, and being an artifact is also what makes the list
    /// erasable under a deletion request while leaving the audit fact that an
    /// enumeration happened permanently intact.
    ///
    /// Field 6: `enumerated`
    #[must_use]
    pub fn enumerated(
        &self,
    ) -> &::buffa::MessageFieldView<super::super::__buffa::view::ArtifactRefView<'_>> {
        &self.0.reborrow().enumerated
    }
}
impl ::core::convert::From<::buffa::OwnedView<ResourceAccessRecordView<'static>>>
for ResourceAccessRecordOwnedView {
    fn from(inner: ::buffa::OwnedView<ResourceAccessRecordView<'static>>) -> Self {
        ResourceAccessRecordOwnedView(inner)
    }
}
impl ::core::convert::From<ResourceAccessRecordOwnedView>
for ::buffa::OwnedView<ResourceAccessRecordView<'static>> {
    fn from(wrapper: ResourceAccessRecordOwnedView) -> Self {
        wrapper.0
    }
}
impl ::core::convert::AsRef<::buffa::OwnedView<ResourceAccessRecordView<'static>>>
for ResourceAccessRecordOwnedView {
    fn as_ref(&self) -> &::buffa::OwnedView<ResourceAccessRecordView<'static>> {
        &self.0
    }
}
impl ::buffa::HasMessageView for super::super::ResourceAccessRecord {
    type View<'a> = ResourceAccessRecordView<'a>;
    type ViewHandle = ResourceAccessRecordOwnedView;
}
impl ::serde::Serialize for ResourceAccessRecordOwnedView {
    fn serialize<__S: ::serde::Serializer>(
        &self,
        __s: __S,
    ) -> ::core::result::Result<__S::Ok, __S::Error> {
        ::serde::Serialize::serialize(&self.0, __s)
    }
}
