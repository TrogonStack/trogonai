skill_id = "secrets-redactor"
wasm_path = "skills/secrets_redactor.wasm"
applies_to_method = { kind = "OneOf", methods = ["message/send", "message/stream", "tasks/get"] }
applies_to_paths = ["$.message.parts[*].text", "$.message.parts[*].content", "$.parts[*].text", "$.parts[*].content"]
category = "Credentials"
version = "1.0.0"
