[package]
name = "secrets-redactor"
version = "0.1.0"
edition = "2024"
publish = false

[lints]
workspace = true

[lib]
crate-type = ["cdylib"]
path = "src/lib.rs"

[dependencies]
regex = { version = "=1.12.2", default-features = false, features = ["std", "unicode-perl", "unicode-case"] }
serde_json = { workspace = true }
tier3-skill-abi = { path = "../tier3-skill-abi" }
