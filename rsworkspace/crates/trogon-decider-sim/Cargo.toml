[package]
name = "trogon-decider-sim"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"
publish = false

[lints]
workspace = true

[features]
test-support = []

[dependencies]
tempfile = "3"
thiserror = { workspace = true }
trogon-decider-wit = { workspace = true, features = ["host"] }
wasmparser = "=0.227.0"
wasmtime = { version = "=29.0.1", default-features = false, features = ["component-model", "runtime", "cranelift"] }

[dev-dependencies]
buffa = { workspace = true }
buffa-types = { workspace = true }
criterion = "0.5"
trogon-decider-wit = { workspace = true, features = ["host"] }
trogonai-proto = { path = "../trogonai-proto", default-features = false, features = ["light", "schedules"] }
wasmtime = { version = "=29.0.1", default-features = false, features = ["component-model", "runtime", "cranelift"] }
wat = "1"

[[bench]]
name = "decider"
harness = false
required-features = ["test-support"]

[[test]]
name = "light"
path = "tests/light.rs"
required-features = ["test-support"]

[[test]]
name = "schedules"
path = "tests/schedules.rs"
required-features = ["test-support"]
