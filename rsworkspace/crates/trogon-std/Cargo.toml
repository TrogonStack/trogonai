[package]
name = "trogon-std"
version = "0.1.0"
edition = "2024"

[lints]
workspace = true

[features]
test-support = []
clap = ["dep:clap"]

[dependencies]
bytesize = "2.3.1"
clap = { workspace = true, optional = true }
serde = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
tempfile = "3"
