[package]
name = "trogon-std"
version = "0.1.0"
edition = "2024"

[lints]
workspace = true

[features]
test-support = []

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
