[package]
name = "trogon-decider"
version = "0.1.0"
edition = "2024"
description = "Typed decider primitives for Trogon event-sourced workflows"
license = "MIT"
repository = "https://github.com/TrogonStack/trogonai"

[lints.rust]
warnings = "deny"
unexpected_cfgs = { level = "deny", check-cfg = ['cfg(coverage)'] }

[lints.clippy]
all = "deny"
expect_used = "deny"
panic = "deny"
unwrap_used = "deny"
