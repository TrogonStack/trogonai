[package]
name = "trogon-light-decider"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"
publish = false

[lib]
crate-type = ["cdylib"]

[lints]
workspace = true

[dependencies]
trogon-decider = { workspace = true }
trogon-decider-guest-sdk = { workspace = true }
wit-bindgen = { workspace = true }
trogonai-proto = { path = "../trogonai-proto", default-features = false, features = ["light"] }

[package.metadata.component]
package = "trogon:decider"
