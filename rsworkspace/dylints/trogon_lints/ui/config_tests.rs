// edition:2024
// A `*_tests.rs` file carries fixtures and per-case tuning values, not crate
// configuration, so a module-level constant here is exempt: the lint must NOT
// fire.
#![allow(dead_code)]

const SAMPLE_BODY_LEN: usize = 32;

fn main() {}
